[project]
name = "yfinance-mcp-server"
version = "0.1.0"
description = "MCP server for US stock market data using Yahoo Finance"
readme = "README.md"
requires-python = ">=3.11.6"
dependencies = [
    "mcp[cli]>=1.9.4",
    "pandas>=2.3.0",
    "yfinance>=0.2.0",
    "python-dotenv>=1.0.0",
]
